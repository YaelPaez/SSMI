
@{
    ViewData["Title"] = "Home Page";
}
<style>
    /* ── Variables y Base ── */
    :root {
        --cian: #00d4ff;
        --azul-boton: #5b58ff;
        --fondo-oscuro: #0d1b2a;
    }

    body {
        margin: 0;
        font-family: 'Segoe UI', system-ui, sans-serif;
        background: linear-gradient(135deg, #0d1b2a 0%, #1b3a4b 100%);
        color: white;
        min-height: 100vh;
    }

    .seccion-nosotros {
        display: flex;
        align-items: center;
        justify-content: space-between;
        min-height: 100vh;
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        gap: 3rem;
    }

    /* ── Texto e Izquierda ── */
    .bloque-texto {
        flex: 1;
        animation: aparecerIzquierda 1s ease;
    }

        .bloque-texto h1 {
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 1rem;
        }

        .bloque-texto p {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 2rem;
        }

    /* ── Botones ── */
    .grupo-botones {
        display: flex;
        flex-direction: column;
        gap: 12px;
        align-items: flex-start;
    }

    .fila-botones {
        display: flex;
        gap: 12px;
        width: 100%;
    }

    .btn-principal {
        background: var(--azul-boton);
        color: white;
        padding: 12px 25px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 700;
        transition: 0.3s;
        text-align: center;
        flex: 1;
    }

    .btn-secundario {
        background: rgba(255,255,255,0.1);
        border: 1px solid rgba(255,255,255,0.2);
        color: white;
        padding: 12px 25px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        transition: 0.3s;
        text-align: center;
        flex: 1;
    }

    .btn-equipo {
        color: var(--cian);
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 600;
        margin-top: 10px;
        display: inline-flex;
        align-items: center;
        gap: 8px;
    }

    .btn-principal:hover, .btn-secundario:hover {
        transform: translateY(-3px);
        filter: brightness(1.2);
    }

    .btn-equipo:hover {
        text-decoration: underline;
    }

    /* ── Contenedor de Imagen (Derecha) ── */
    .bloque-visual {
        flex: 1.2;
        animation: aparecerDerecha 1.2s ease;
    }

    .contenedor-imagen {
        width: 100%;
        height: 450px;
        border-radius: 30px;
        overflow: hidden;
        border: 2px solid rgba(255,255,255,0.1);
        box-shadow: 0 25px 50px rgba(0,0,0,0.3);
        background: var(--fondo-oscuro);
    }

    .mi-imagen {
        width: 100%;
        height: 100%;
        object-fit: cover; /* Esto hace que tu imagen se adapte sin deformarse */
    }

    /* ── Animaciones ── */
    @@keyframes aparecerIzquierda {
        from {
            opacity: 0;
            transform: translateX(-40px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @@keyframes aparecerDerecha {
        from {
            opacity: 0;
            transform: translateX(40px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    /* ── Responsive ── */
    @@media (max-width: 992px) {
        .seccion-nosotros {
            flex-direction: column;
            text-align: center;
            padding-top: 4rem;
        }

        .grupo-botones {
            align-items: center;
        }

        .contenedor-imagen {
            height: 300px;
        }
    }
</style>

<div class="seccion-nosotros">
    <div class="bloque-texto">
        <h1>SSMI</h1>
        <p>
            Con esta aplicacion, pretendemos conectar a las personas con su destino a través de tecnología inteligente. <br />
            Rutas optimizadas, transporte en tiempo real y un enfoque sostenible.
        </p>

        <div class="grupo-botones">
            <div class="fila-botones">
                <a asp-controller="Login" asp-action="Registro" class="btn-principal">Registrarse</a>
                <a asp-controller="Login" asp-action="IniciarSesion" class="btn-secundario">Iniciar Sesión</a>
            </div>

            <a asp-controller="Home" asp-action="Desarrolladores" class="btn-equipo">
                <i class="bi bi-people-fill"></i> Conoce al equipo de desarrolladores
            </a>
        </div>
    </div>
    <div class="bloque-visual">
        <div class="contenedor-imagen">
            <img src=""
                 alt="logo de la aplicacion, pero no lo encontrexd"
                 class="mi-imagen" />
        </div>
    </div>
</div>